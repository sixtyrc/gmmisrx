# Python / Django
filter_app/venv/
__pycache__/
*.pyc
filter_app/db.sqlite3

# Archivos generados en runtime (contienen datos de afiliados reales)
filter_app/tmp_downloads/*
filter_app/padron_archivos/*
logs/

# Export de GeneXus: se versiona el .xpz, no el XML descomprimido (derivado, ~37MB)
gx/_extract_afiliado/
gx/_extract_reporte/

# Datos reales de afiliados (DNI/nombres) usados para validar el cruce: nunca versionar
gx/*.xlsx
gx/*.csv

# Secretos
.env
*.env
/data.txt
