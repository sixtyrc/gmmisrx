# Python / Django
filter_app/venv/
__pycache__/
*.pyc
filter_app/db.sqlite3

# Archivos generados en runtime (contienen datos de afiliados reales)
filter_app/tmp_downloads/*
filter_app/padron_archivos/*
logs/
filter_app/staticfiles/

# Carpeta de trabajo con datos reales de afiliados (DNI/nombres), exports de GeneXus
# y manuales internos de MisRx: nunca versionar nada de aca.
# Nota: gx/afiliado.xpz y gx/reportecsv.xpz ya estaban trackeados de antes de esta regla,
# .gitignore no los saca del historial (hace falta `git rm --cached` aparte si se quiere).
gx/

# Secretos
.env
*.env
/data.txt
